# =============================================================================
# Anso Web - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
#
# Note: In development, the Vite dev server proxies /api requests to the backend.
# In production, configure your hosting to proxy /api to the backend URL.

# -----------------------------------------------------------------------------
# API Configuration (Optional)
# -----------------------------------------------------------------------------
# Only needed if not using the default proxy configuration
# VITE_API_URL=http://localhost:3000
